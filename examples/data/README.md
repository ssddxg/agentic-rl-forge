# Example data

These tiny records exist only to exercise the offline pipeline, retriever, split manifests, and
CLI examples. They are not a benchmark and must not be used to claim model quality.

- `qa.jsonl` contains two exact-match questions.
- `corpus.jsonl` contains supporting passages and plausible distractors.
